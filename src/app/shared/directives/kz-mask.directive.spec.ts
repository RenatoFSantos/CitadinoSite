import { KzMaskDirective } from './kz-mask.directive';

describe('KzMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new KzMaskDirective();
    expect(directive).toBeTruthy();
  });
});
