import { KzMaskCurrencyDirective } from './kz-mask-currency.directive';

describe('KzMaskCurrencyDirective', () => {
  it('should create an instance', () => {
    const directive = new KzMaskCurrencyDirective();
    expect(directive).toBeTruthy();
  });
});
