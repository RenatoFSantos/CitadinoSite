import { KzCpfDirective } from './kz-cpf.directive';

describe('KzCpfDirective', () => {
  it('should create an instance', () => {
    const directive = new KzCpfDirective();
    expect(directive).toBeTruthy();
  });
});
